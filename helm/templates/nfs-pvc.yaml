kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: {{ .Chart.Name }}-pvc-claim
spec:
  storageClassName: {{ .Values.storageClassName.storage }}
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi